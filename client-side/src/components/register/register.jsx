import React from 'react';
import RegisterForm from './register-form';

const Register = ({ className }) => (
    <section className="register">
        <h1 className="register-title">
            Idk, some motivational batka text..
        </h1>
        <RegisterForm />
    </section>
);

export default Register;
