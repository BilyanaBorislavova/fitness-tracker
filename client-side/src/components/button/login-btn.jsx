import React from 'react';
import GenericBtn from './generic-btn';

const LoginBtn = () => (
    <GenericBtn
      text="Login"
      onClick={() => {}}
      isPrimary={false}
    />
);

export default LoginBtn;
